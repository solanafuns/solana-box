{
  "name": "solana-deploy-action",
  "version": "1.0.0",
  "description": "GitHub Action to deploy Solana programs",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build scripts/index.js -o dist --source-map --license licenses.txt",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "solana",
    "github-actions",
    "deploy"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/io": "^1.1.2"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}

